# @open-report/core

Runtime and CLI for [open-report](https://github.com/open-report/open-report) — write reports in `reports/`, we handle pagination, citations, numbering, and export.

Scaffold a project with:

```bash
npx @open-report/cli init my-report
```
